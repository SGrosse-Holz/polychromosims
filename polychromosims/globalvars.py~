# A file containing global vars to be used for controlling execution and stuff
params_allow_execution = False # Prevent params.py from doing anything that
                               # modifies things outside itself, such as
                               # creating or emptying a directory
params_write_processed = True # Save processed params.py
